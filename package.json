{
  "name": "secret-drop",
  "version": "1.0.0",
  "private": true,
  "description": "A localized Cloudflare Worker password sending service with self-destructing links.",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "build": "tsc --outDir dist --noEmit false",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "devDependencies": {
    "typescript": "^5.8.3",
    "wrangler": "^4.18.0"
  }
}
